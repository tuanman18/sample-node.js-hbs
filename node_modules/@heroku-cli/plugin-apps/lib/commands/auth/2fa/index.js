'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _command = require('@heroku-cli/command');

var _cliUx = require('cli-ux');

class Index extends _command.Command {

  async run() {
    let { body: account } = await this.heroku.get('/account');
    if (account.two_factor_authentication) {
      _cliUx.cli.log('Two-factor authentication is enabled');
    } else {
      _cliUx.cli.log('Two-factor authentication is not enabled');
    }
  }
}
exports.default = Index;
Index.topic = 'auth';
Index.command = '2fa';
Index.description = 'check 2fa status';
Index.aliases = ['2fa', 'twofactor'];